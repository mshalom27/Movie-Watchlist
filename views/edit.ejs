<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body class="bg-black text-white min-h-screen flex items-center justify-center py-10">

  <div class="w-full max-w-2xl px-6">

    <!-- Back Button -->
    <div class="flex justify-end mb-6">
      <a href="/profile" class="bg-red-600 hover:bg-red-500 text-white px-4 py-2 rounded-md shadow-sm transition">
        Back to Profile
      </a>
    </div>

    <!-- Form Title -->
    <h2 class="text-3xl font-bold text-yellow-400 mb-8">Edit Movie</h2>

    <!-- Edit Movie Form -->
    <form action="/update/<%= movie._id %>" method="POST" class="space-y-5 bg-zinc-900 border border-zinc-700 p-6 rounded-2xl shadow-md">

      <input 
        type="text" 
        name="title" 
        placeholder="Movie Title" 
        value="<%= movie.title %>" 
        required
        class="w-full px-4 py-3 bg-zinc-800 border border-zinc-700 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-400"
      />

      <input 
        type="text" 
        name="url" 
        placeholder="Poster URL" 
        value="<%= movie.url %>" 
        required
        class="w-full px-4 py-3 bg-zinc-800 border border-zinc-700 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-400"
      />

      <input 
        type="text" 
        name="director" 
        placeholder="Director" 
        value="<%= movie.director %>" 
        class="w-full px-4 py-3 bg-zinc-800 border border-zinc-700 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-400"
      />

      <input 
        type="text" 
        name="genre" 
        placeholder="Genre" 
        value="<%= movie.genre %>" 
        class="w-full px-4 py-3 bg-zinc-800 border border-zinc-700 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-400"
      />

      <input 
        type="number" 
        name="year" 
        placeholder="Year" 
        value="<%= movie.year %>" 
        min="1888" 
        max="2100"
        class="w-full px-4 py-3 bg-zinc-800 border border-zinc-700 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-400"
      />

      <textarea 
        name="notes" 
        placeholder="Notes (Why this movie?)" 
        rows="4"
        class="w-full px-4 py-3 bg-zinc-800 border border-zinc-700 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-400 resize-none"
      ><%= movie.notes %></textarea>

      <div class="text-center">
        <input 
          type="submit" 
          value="Update Movie" 
          class="bg-yellow-400 hover:bg-yellow-300 text-black font-semibold px-6 py-3 rounded-md shadow-md transition"
        />
      </div>

    </form>

  </div>
</body>

</html>