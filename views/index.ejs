<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body class="bg-gradient-to-br from-zinc-900 to-black text-white min-h-screen flex items-center justify-center px-4">
  <div class="w-full max-w-md bg-zinc-800 p-8 rounded-2xl shadow-xl border border-zinc-700">
    
    <% if (message) { %>
      <p class="text-red-400 mb-4 text-center font-medium"><%= message %></p>
    <% } %>

    <h2 class="text-3xl font-bold mb-6 text-center">Create Account</h2>

    <form action="/register" method="post" class="space-y-4">
      <input
        type="text"
        name="username"
        placeholder="Username"
        class="w-full px-4 py-3 rounded-lg bg-zinc-700 placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500"
      />

      <input
        type="text"
        name="name"
        placeholder="Full Name"
        class="w-full px-4 py-3 rounded-lg bg-zinc-700 placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500"
      />

      <input
        type="email"
        name="email"
        placeholder="Email"
        class="w-full px-4 py-3 rounded-lg bg-zinc-700 placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500"
      />

      <input
        type="password"
        name="password"
        placeholder="Password"
        class="w-full px-4 py-3 rounded-lg bg-zinc-700 placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500"
      />

      <input
        type="number"
        name="age"
        placeholder="Age"
        class="w-full px-4 py-3 rounded-lg bg-zinc-700 placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500"
      />

      <button
        type="submit"
        class="w-full bg-blue-600 hover:bg-blue-500 transition duration-200 text-white font-semibold py-3 rounded-lg shadow-md"
      >
        Create Account
      </button>
    </form>
  </div>
</body>
</html>